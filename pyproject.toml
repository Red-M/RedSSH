[tool.poetry]
name = "RedSSH"
version = "2.0.1"
description = "SSH automation for easy of use and all the extra options"

license = "Linux GPLv2"

authors = [
    "Red_M <redssh@red-m.net>"
]

readme = 'README.md'  # Markdown files are supported

repository = "https://bitbucket.org/Red_M/RedSSH"
homepage = "https://bitbucket.org/Red_M/RedSSH"

keywords = ['automation', 'SSH', 'expect', 'paramiko', 'SFTP', 'ssh', 'sftp']

[tool.poetry.dependencies]
python = ">2.8"  # Compatible python versions must be declared here
paramiko_expect = "*"
paramiko = "*"

